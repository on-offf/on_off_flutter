import 'package:flutter/widgets.dart';

class BorderConstants {
  static const bottomSheet = BorderRadius.all(Radius.circular(5.0));
  static const dailyBody = BorderRadius.all(Radius.circular(5.0));
  static const feature = BorderRadius.all(Radius.circular(5.0));
}
