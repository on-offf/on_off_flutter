import 'package:flutter/material.dart';

class OnHomeScreen extends StatelessWidget {
  static const routeName = '/on/home';

  const OnHomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
